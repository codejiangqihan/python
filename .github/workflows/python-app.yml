# 当有改动推送到master分支时，启动Action
name: 自动部署

on:
  push:
    branches:
      - main

  release:
    types:
      - published

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 安装 Python 3.7
        uses: actions/setup-python@v1
        with:
          python-version: 3.7
      - name: 安装python库
        run: |
          pip isntall gopup
      - name: 运行代码
        run: |
          curl -o main.py https://raw.githubusercontent.com/codejiangqihan/python/main/main.py
          python main.py
      - name: 打包
        uses: actions/upload-artifact@v1
        with: 
          name: release-pypi.txt
          path: .
